services:
  app:
    build: .
    container_name: donorconnect_app
    env_file:
      - .env
    ports:
      - "3000:3000"
    # Do not restart endlessly in CI; just for local development you can add:
    restart: unless-stopped
